# 功能模块目录

- **生成时间**：[DATE]

<!-- 关键规则/特别注意：我们要生成的原始需求内容描述的系统中所有功能的功能模块目录，不是仅仅是系统的单个功能模块，禁止只生成单个功能模块的功能模块目录。 -->
<!-- 关键规则/特别注意/严格遵守：按照以下所有步骤创建任务清单（TodoList）生成功能模块目录： -->
<!-- 步骤一：阅读并充分理解原始需求，生成并输出 `多结构目录结构功能点集合`。**特别注意**：必须包含系统内置功能模块：登录页、无权限页、权限中心（用户管理、角色管理、权限管理、部门管理）。 -->
<!-- 步骤二：将 原始需求中的多个多层级目录结构功能点 在不遗漏的前提下，拆解出所有单独功能点。将拆解得到的功能点与 `多结构目录结构功能点集合` 进行对比，如果出现任何或遗漏伪造功能点，都要重新生成 `多结构目录结构功能点集合`，直到原始需求中的多个多层级目录结构功能点 与 `多结构目录结构功能点集合` 完全匹配为止。**特别注意**：确保内置功能模块已被正确包含在集合中。 -->
<!-- 步骤三：将 `多结构目录结构功能点集合` 在不遗漏的前提下，拆解出所有单独功能点。将拆解得到的功能点与 原始需求中的多个多层级目录结构功能点 进行对比，如果出现任何或遗漏伪造功能点，都要重新生成 `多结构目录结构功能点集合`，直到 `多结构目录结构功能点集合` 与 原始需求中的多个多层级目录结构功能点 完全匹配为止。 -->
<!-- 步骤四：在完全阅读并充分理解 `常见错误一`、`常见错误三`、`常见错误四`、`常见错误五`、`常见错误十五` 的情况下，必须针对每条常见错误对已生成的 `多结构目录结构功能点集合` 进行逐一验证，如果出现任何常见错误，必须重新生成 `多结构目录结构功能点集合` ，直到完全规避所有常见错误为止。**特别注意**：必须检查原始需求中是否存在与登录、权限相关的内容，如果存在，必须确保这些内容都被正确地整合到内置功能模块中，禁止创建重复的登录或权限功能点。 -->
<!-- 步骤五：在完全阅读并充分理解 `功能模块目录`、`功能级数`、`功能类别` 的情况下，参考 `示例表头` 、`示例表体`、`示例表格` ，暂时忽略功能隐藏，生成功能模块目录到目标文件中，**必须生成指定目标文件**，禁止遗漏/忽略/隐藏/伪造任何功能，禁止填充不存在的功能。 -->
<!-- 步骤六：在完全阅读并充分理解所有 `常见错误` 的情况下，必须针对每条常见错误对已生成的功能模块目录严格进行逐一验证，如果出现任何常见错误，必须重新生成功能模块目录，直到完全规避所有常见错误为止。**特别注意**：必须重点检查常见错误二十一、二十二、二十三，确保原始需求中所有与登录、权限相关的内容都已统一收纳到内置功能模块中，禁止出现重复的登录或权限功能点。 -->
<!-- 步骤七：在完全阅读并充分理解所有 `验收项` 的情况下，必须针对每条验收项对已生成的功能模块目录严格进行逐一验证，如果出现任何不符合任何验收项的情况，必须重新生成功能模块目录，直到完全符合所有验收项为止。 -->
<!-- 步骤八：在完全阅读并充分理解所有 `功能隐藏` 的情况下，必须针对每条功能隐藏对功能模块目录所有功能点进行隐藏处理，直到完全已经处理所有需要隐藏的功能点为止。 -->
<!-- 步骤九：在完全阅读并充分理解 `功能隐藏二` 的情况下，针对功能隐藏二必须逐个对功能模块目录每个功能层级的每个功能点进行单独的隐藏判断处理，禁止遗漏功能模块目录的任何功能点，处理完成之后要必须再次逐个对功能模块目录每个功能点进行单独的隐藏判断处理，直到完全已经处理所有需要隐藏的功能点为止。 -->
<!-- 步骤十：在完全阅读并充分理解 `功能隐藏三` 的情况下，针对功能隐藏三必须逐个对功能模块目录每个功能层级的每个功能点进行单独的隐藏判断处理，禁止遗漏功能模块目录的任何功能点，处理完成之后要必须再次逐个对功能模块目录每个功能点进行单独的隐藏判断处理，直到完全已经处理所有需要隐藏的功能点为止。 -->
<!-- 步骤十一：在完全阅读并充分理解 `功能隐藏四` 的情况下，针对功能隐藏四必须逐个对功能模块目录每个功能层级的每个功能点进行单独的隐藏判断处理，禁止遗漏功能模块目录的任何功能点，处理完成之后要必须再次逐个对功能模块目录每个功能点进行单独的隐藏判断处理，直到完全已经处理所有需要隐藏的功能点为止。 -->
<!-- 步骤十二：在完全阅读并充分理解 `功能隐藏五` 的情况下，针对功能隐藏五必须逐个对功能模块目录每个功能层级的每个功能点进行单独的隐藏判断处理，禁止遗漏功能模块目录的任何功能点，处理完成之后要必须再次逐个对功能模块目录每个功能点进行单独的隐藏判断处理，直到完全已经处理所有需要隐藏的功能点为止。 -->
<!-- 步骤十三：在完全阅读并充分理解 `常见错误七`、`常见错误九`、`常见错误十`、`常见错误十一`、`常见错误十二`、`常见错误十三`、`常见错误十四`、`常见错误十五`、`常见错误十六`、`常见错误二十一`、`常见错误二十二`、`常见错误二十三` 的情况下， 特别检查这些常见错误，如果出现任何常见错误的类似问题，必须重新生成功能模块目录，直到完全规避所有常见错误为止。 -->
<!-- 步骤十四：在完全阅读并充分理解 `功能隐藏二`、`功能隐藏三` 的情况下，针对功能隐藏二与功能隐藏三必须逐个对功能模块目录每个功能点进行单独的隐藏判断处理，禁止遗漏功能模块目录的任何功能点，处理完成之后要必须再次逐个对功能模块目录每个功能点进行单独的隐藏判断处理，直到完全已经处理所有需要隐藏的功能点为止。 -->
<!-- 步骤十五：在完全阅读并充分理解 `功能隐藏六`、`功能隐藏七` 的情况下，针对功能隐藏六与功能隐藏七必须逐个对功能模块目录每个功能点进行单独的隐藏判断处理，禁止遗漏功能模块目录的任何功能点，处理完成之后要必须再次逐个对功能模块目录每个功能点进行单独的隐藏判断处理，直到完全已经处理所有需要隐藏的功能点为止。 -->
<!-- 步骤十六：阅读并充分理解原始需求，判断原始需求或用户输入是否有明确表示忽略/隐藏某些功能模块。如果用户有明确表示忽略/隐藏某些功能模块，对功能模块目录进行对应修改。如果用户没有明确表示忽略/隐藏某些功能模块，禁止对功能模块目录进行任何修改。 -->

<!-- 功能模块目录：`多结构目录结构功能点集合` 的所有 `页面` 功能点。 -->

<!-- 功能级数：多级功能结构组织的层级不明确，必须包含所有层级的功能点，禁止遗漏或伪造功能级数。如果原始需求中没有明确说明功能的层级结构，一般情况功能级数至少为两级或两级以上。原始需求中可能出现多级功能级数，对此没有级数上限。一级功能也至少有两个及以上。 -->

<!-- 功能类别：功能类别只有两种：`页面` 和 `逻辑`。`页面` 是当前功能点更适合作为页面来实现。`逻辑` 表示当前功能点不需要给用户可操作的交互节点，后端逻辑既可处理。-->

<!-- 特别注意：**目前忽略所有的 `逻辑` 功能点**，功能模块目录禁止包含任何 `逻辑` 功能点。 -->

<!-- 最底层功能层级的功能点：通过 `多结构目录结构功能点集合` 整合出初次生成功能模块目录中有多少个功能级数（与后续修改生成的 功能模块目录 的功能级数无关） 比如：整理出 一级功能 到 八级功能，这就有八个功能级数。这种情况下，八级功能 就是 最底层功能层级，当 某功能点 的 功能层级 是 八级功能 时，这个功能点就是 最底层功能层级的功能点。以上情况不局限于`八级功能`，适用于任何 `功能级数`。-->

<!-- 功能隐藏一：特别注意：隐藏不是忽略了功能点对应的需求，被隐藏的子级功能点的需求内容会被整合进父级功能点的需求描述进行实现。 -->

<!-- 功能隐藏二：如果某功能层级的功能点符合以下所有条件就必须被隐藏：禁止隐藏任何一级功能；当前功能点的父级的所有子功能点中的出现 组件操作、业务组件、信息内容、交互、布局、样式、字体、菜单、表单、查看、展示、弹窗 中的任何一类功能节点。 -->
<!-- 功能隐藏二 示例：比如：八级功能-新增页面、九级功能-标签容器、九级功能-选择分类、九级功能-身份信息。九级功能-标签容器 明显属于 业务组件，九级功能-选择分类 明显属于元素操作，九级功能-身份信息 明显属于信息内容。这种情况下 八级功能-新增页面 下的所有子功能点都必须隐藏，八级功能-新增页面 下就不会有任何子功能点。以上情况不局限于`八级功能`、`九级功能`，适用于任何 `功能级数`。 -->

<!-- 功能隐藏三：如果某功能层级的功能点符合以下所有条件就必须被隐藏：禁止隐藏任何一级功能；当前功能点的父级功能点在原始需求中明确表示为 **页面或页面元素**。符合以上所有条件的功能点，它们父级功能点的功能类别必须修改为 `页面`，并且它们父级功能点的所有子级功能点都必须隐藏。 -->
<!-- 功能隐藏三 示例：比如：八级功能-新增页面、九级功能-交互、九级功能-表单部分、十级功能-选择分类。八级功能-新增页面 在原始需求中明确表示为一个页面。这种情况下 八级功能-新增页面 下的子功能点都必须隐藏，八级功能-新增页面 下就不会有任何子功能点。以上情况不局限于`八级功能`、`九级功能`、`十级功能`，适用于任何 `功能级数`。 -->

<!-- 功能隐藏四：如果某功能层级的功能点符合以下所有条件就必须被隐藏：当前功能点的功能分类必须是页面；当前功能点的功能分类必须是 `最底层功能层级的功能点`；当前功能层级中存在被隐藏的功能点；当前功能点的父级是 综合、统计、图表、查询、首页 等类似聚合展示的功能点。 -->
<!-- 功能隐藏四 示例：比如：八级功能-大盘、九级功能-出售数据、九级功能-退单数据，这种情况 必须隐藏 九级功能-出售数据、九级功能-退单数据。特别注意，如果当前层级没有被隐藏的页面增删改查功能点，禁止对这个功能点进行隐藏操作。 -->

<!-- 功能隐藏五：如果某功能层级的功能点符合以下所有条件就必须被隐藏：当前功能点的功能分类必须是页面；当前功能点的父级已经有被隐藏的子功能点。 -->

<!-- 功能隐藏六：如果某功能层级的功能点符合以下所有条件就必须被隐藏：当前功能点的功能分类必须是页面；当前功能点描述的是页面组件元素。比如：“导航”、“菜单”、“弹窗” 等类似功能点，这些功能点描述的是页面内的组件元素，不是页面的完整业务功能，所以类似页面功能点都必须被隐藏。特别注意：功能隐藏六 适用于任何功能层级的页面功能点（包含一级功能、二级功能等所有层级的页面功能点），不仅仅适用于 `最底层功能层级的功能点`。 -->

<!-- 功能隐藏七：如果某功能层级的功能点符合以下所有条件就必须被隐藏：当前功能点的功能分类必须是页面；当前功能点描述的是交互规范、页面设计等内容。比如：“视觉”、“交互”、“主题”、“样式”、“美观” 等类似功能点，这些功能点描述的是页面内交互设计，不是页面的完整业务功能，所以类似页面功能点都必须被隐藏。特别注意：功能隐藏七 适用于任何功能层级的页面功能点（包含一级功能、二级功能等所有层级的页面功能点），不仅仅适用于 `最底层功能层级的功能点`。 -->

<!-- 多结构目录结构功能点集合：阅读并充分理解原始需求，找到原始需求中的全部（一个或多个，禁止遗漏任何一个）类似多层级目录结构功能点。必须完整理解每个多层级目录结构功能点内容，然后输出当前多层级目录结构功能点中的所有顶级功能点/一级功能点（禁止使用“等”这种描述进行忽略）。之后再将多个多层级目录结构功能点**合并**得到并输出多层级目录结构功能点的**集合**，合并过程中，**重点关注/特别注意**每个多层级目录结构功能点的差异部分是否被合并保留，禁止遗漏或者伪造任何功能点。`多结构目录结构功能点集合`是一份最多最全的多层级目录结构功能点，这是生成当前文件的内容的数据基础，以及后续检查是否遗漏功能点的基础。充分理解 `常见错误三`、`常见错误四`、`常见错误五`，生成 `多结构目录结构功能点集合`前都要仔细检查，禁止出现类似错误。充分理解 `常见错误三`、`常见错误四`、`常见错误五`，生成 `多结构目录结构功能点集合`后都要仔细检查，禁止出现类似错误。 -->

<!-- 特别注意：多结构目录结构功能点集合必须要关注 原始纲要，很可能也属于包含业务的 **多层级目录结构功能点**，非常适合用于补充功能点。 -->

<!-- 验证项一：生成完成后，必须仔细思考所有 `功能隐藏` 项，逐个分析所有功能点。如果出现某些匹配功能点没有被正常隐藏，必须重新生成功能模块目录，直到隐藏所有匹配到的功能点为止。 -->
<!-- 验证项二：生成完成后，重新阅读并分析原始需求中的 `多结构目录结构功能点集合`，必须严格比对生成的功能模块目录是否完整包含的原始需求中的`多结构目录结构功能点集合`的功能点。如果出现任何遗漏或者伪造的任何层级的功能点，必须重新生成功能模块目录。直到生成的功能模块目录完整包含的原始需求中的`多结构目录结构功能点集合`的功能点为止。 -->
<!-- 验证项三：生成完成后，必须检查功能模块目录是否包含所有系统内置功能模块：登录页、无权限页、权限中心（包含用户管理、角色管理、权限管理、部门管理）。如果缺少任何一个内置功能模块，必须重新生成功能模块目录，直到包含所有内置功能模块为止。 -->
<!-- 验证项四：生成完成后，必须检查权限中心的所有子功能点（用户管理、角色管理、权限管理、部门管理）的功能类别是否都标记为 `页面`。如果出现错误的功能类别标记，必须重新生成功能模块目录，直到所有权限中心子功能点都正确标记为 `页面` 为止。 -->
<!-- 验证项五：生成完成后，必须检查功能模块目录中是否存在与登录、权限相关的重复功能点。比如：是否同时存在"登录"和"三方登录"、"OAuth登录"等多个登录功能点；是否同时存在"权限中心"和"权限管理"、"权限配置"等多个权限功能点。如果出现任何重复的登录或权限功能点，必须重新生成功能模块目录，将所有相关内容统一收纳到内置功能模块中，直到完全消除所有重复为止。 -->
<!-- 验证项六：生成完成后，必须重新阅读原始需求，检查是否存在与登录、权限、用户管理、角色管理、部门管理等相关的功能描述。如果存在这些相关内容，必须确保它们都已被正确地整合到对应的内置功能模块中。如果出现任何遗漏或独立的登录/权限功能点，必须重新生成功能模块目录，直到所有相关内容都被统一收纳为止。 -->

<!-- 常见错误一：只生成了原始需求一个子系统的功能点，没有生成原始需求内容描述的所有功能点。禁止因为任何标题元素，导致没有生成原始需求内容描述的所有功能点。 -->
<!-- 常见错误二：没有严格按照 `多结构目录结构功能点集合` 生成功能模块目录，导致出现遗漏或伪造了某个多层级结构中功能点的情况。功能模块目录必须严格按照 `多结构目录结构功能点集合` 生成功能模块目录，禁止因为原始需求内任何标题描述导致，只生成了 `多结构目录结构功能点集合` 内的一个子系统的功能点，没有严格生成全部功能点。 -->
<!-- 常见错误三：原始需求中存在多个多结构目录结构功能点，比如：`多结构目录结构功能点一` 和 `多结构目录结构功能点二`，某些功能点只在 `多结构目录结构功能点二` 中出现，导致生成的 `多结构目录结构功能点集合` 出现问题，遗漏了只在 `多结构目录结构功能点二` 出现的功能点。这是一个经常出现的严重错误，必须特别关注每个 `多结构目录结构功能点`的差异，保证这些差异点不被遗漏，必须在生成任何内容后都要检查修复。 -->
<!-- 常见错误四：原始需求中存在多个多结构目录结构功能点，比如：`多结构目录结构功能点一` 和 `多结构目录结构功能点二`，`多结构目录结构功能点二` 的功能点与 `多结构目录结构功能点一` 的某功能点的子功能点高度重合，理论上应该作为 `多结构目录结构功能点一` 的某功能点的子功能点补充，但是 `多结构目录结构功能点二` 的顶级功能点错误的被当作一级功能出现在 `多结构目录结构功能点集合` 中。这是一个严重错误，必须在生成任何内容后都要检查修复。理想情况应该是，仔细比对 `多结构目录结构功能点一` 的所有功能点，判断 `多结构目录结构功能点二` 是否与 `多结构目录结构功能点一` 的所有层级的子级功能点高度重合匹配，如果出现高度重合匹配的情况，`多结构目录结构功能点二`必须作为 `多结构目录结构功能点一` 的子级功能点补充进行操作。 -->
<!-- 常见错误五：对于原始需求中组成多结构目录结构功能点的长表格内容理解补全，经常出现只收集了最开始出现顶级功能/一级功能，遗漏了后面的所有功能点。这是一个严重错误，必须在生成任何内容后都要检查修复。 -->
<!-- 常见错误六：从原始需求中的描述提取出了某些一级功能，但是该一级功能已经被已经被功能模块目录中的某些子级功能点覆盖。这样根据原始需求中的描述提取出了某些一级功能必须被移除。这是一个严重错误，必须在生成任何内容后都要检查修复。 -->
<!-- 常见错误七：如果功能层级下所有功能都已经被隐藏。这样的功能层级必须被移除。比如：错误示例（`| 一级功能 | ... | X级功能 | 功能类别 | \n |---------| ... |---------|---------| \n | 商品管理 | ... |  | 页面 | \n`），这种示例中尽管表头有 `X级功能`，但是表体内容所有 `X级功能` 相关的表格项都是空的/已经被隐藏，这种情况就必须从表头、表体中移除 `X级功能`，因为 `X级功能` 已经没有任何实际数据。改正后正确示例（`| 一级功能 | ... | 功能类别 | \n |---------| ... |---------| \n | 商品管理 | ... | 页面 | \n`）。这是一个严重错误，必须在生成任何内容后都要检查修复。  -->
<!-- 常见错误八：功能模块目录中出现 `逻辑` 功能点，正确做法是功能模块目录中的禁止出现任何 `逻辑` 相关功能点。 -->
<!-- 常见错误九：没有正常进行功能隐藏处理，功能模块目录中仍存在 修改、删除等页面通用功能。 -->
<!-- 常见错误十：功能模块目录中出现 `逻辑` 功能点，正确做法是功能模块目录中的禁止出现任何 `逻辑` 相关功能点。 -->
<!-- 常见错误十一：功能模块目录中对 `最底层功能层级的功能点` 之外的功能点进行了隐藏。比如：`多结构目录结构功能点集合` 中明确表示最底层功能层级是 `八级功能`，规则限制只能对功能层级为 `八级功能` 的功能点进行隐藏。但是生成的功能模块目录中隐藏了功能层级为 `七级功能` 的功能点。这是一种非常严重的错误，功能模块目录中进行任何隐藏都要检查是否出现类似错误。以上情况不局限于 `七级功能`、`八级功能`，适用于任何 `功能级数` -->
<!-- 常见错误十二：禁止在功能层级的 `表体` 中填充 `多结构目录结构功能点集合` 中不存在的功能点。比如：`多结构目录结构功能点集合` 的功能级数最多有 `五级功能`，当时某个四级功能点没有五级的子功能点，这种情况功能模块目录的中不需要在四级功能点下填充任何五级功能点。示例：`| 首页 | 商品管理 | 列表 | 详情 |  | 页面 | ` 示例中五级功能就是空的，没有填充任何内容。以上情况不局限于`四级功能`、`五级功能`，适用于任何 `功能级数`。 -->
<!-- 常见错误十三：功能模块目录中以 `逻辑` 功能点为主，缺少使用这些 `逻辑` 功能点 的 `页面` 功能点。系统是以 `页面` 功能点设计实现的，缺少 `页面` 功能点，会导致系统设计出现大范围功能遗漏。 -->
<!-- 常见错误十四：功能模块目录中功能名称禁止出现中文、英文、数字以外的任何特殊字符。 -->
<!-- 常见错误十五：多结构目录结构功能点集合 生成过程没有整合 原始纲要，导致功能点出现遗漏。 -->
<!-- 常见错误十六：将权限角色错当作功能点写入功能模块目录中，导致出现大量重复的功能点。正确思路：权限角色属于无用功能点，我们需要提取它的所有子级功能点生成权限目录。 -->

<!-- 常见错误十七：遗漏系统内置功能模块。系统必须包含以下内置功能模块：登录页、无权限页、权限中心（包含用户管理、角色管理、权限管理、部门管理）。这些功能模块是系统的基础设施，禁止遗漏。 -->
<!-- 常见错误十八：权限中心的子功能点不完整。权限中心必须包含以下完整的子功能点：用户管理、角色管理、权限管理、部门管理。禁止遗漏任何一个子功能点。 -->
<!-- 常见错误十九：登录页和无权限页的功能类别错误。登录页和无权限页都必须标记为 `页面` 功能类别，禁止标记为其他类别。 -->
<!-- 常见错误二十：权限中心的功能类别错误。权限中心的所有子功能点（用户管理、角色管理、权限管理、部门管理）都必须标记为 `页面` 功能类别，禁止标记为其他类别。 -->

<!-- 常见错误二十一：原始需求中存在与登录、权限相关的功能描述，但没有统一收纳到内置功能模块中。比如：原始需求中提到"三方登录"、"OAuth登录"、"权限验证"、"角色权限"、"权限配置"等相关内容，这些都必须作为登录页或权限中心的子功能点进行整合，禁止在功能模块目录中创建独立的登录或权限相关功能点。这是一个严重错误，必须在生成任何内容后都要检查修复。 -->

<!-- 常见错误二十二：功能模块目录中出现多个登录相关的功能点。比如：既有"登录"，又有"三方登录"、"OAuth登录"等独立的登录功能点。正确做法是所有登录相关的功能都必须统一在"登录"这个一级功能下进行组织，禁止创建重复的登录功能点。这是一个严重错误，必须在生成任何内容后都要检查修复。 -->

<!-- 常见错误二十三：功能模块目录中出现多个权限相关的功能点。比如：既有"权限中心"，又有"权限管理"、"权限配置"等独立的权限功能点。正确做法是所有权限相关的功能都必须统一在"权限中心"这个一级功能下进行组织，禁止创建重复的权限功能点。这是一个严重错误，必须在生成任何内容后都要检查修复。 -->

<!-- 特别注意：生成内容前，阅读并充分理解原始需求，找到原始需求中的全部（一个或多个，禁止遗漏任何一个）类似多层级目录结构功能点。将多个多层级目录结构功能点**合并**得到并输出多层级目录结构功能点的**并集**，这是生成当前文件的内容的数据基础，以及后续检查是否遗漏功能点的基础，禁止跳过这一步骤。 -->

<!-- 特别注意：系统内置功能模块处理规则。系统必须包含以下内置功能模块，这些模块是系统的基础设施，必须在功能模块目录中明确列出：
1. **登录页**：一级功能，功能类别为 `页面`。用于用户身份验证和系统访问入口。
   - 原始需求中所有与登录相关的内容都必须统一收纳到此功能点中，包括但不限于：本地登录、三方登录、OAuth登录、LDAP登录、SSO登录、扫码登录等所有登录方式。
   - 禁止在功能模块目录中创建独立的"三方登录"、"OAuth登录"等重复的登录功能点。
   - 所有登录相关的功能都应作为登录页的实现细节，而不是独立的功能模块。

2. **无权限页**：一级功能，功能类别为 `页面`。用于展示用户无权限访问的提示信息。

3. **权限中心**：一级功能，包含以下二级功能，所有子功能点功能类别都必须为 `页面`：
   - 用户管理：二级功能，功能类别为 `页面`。用于管理系统用户信息、创建、编辑、删除用户。
     - 原始需求中所有与用户管理相关的内容都必须统一收纳到此功能点中。
   - 角色管理：二级功能，功能类别为 `页面`。用于管理系统角色、分配权限给角色。
     - 原始需求中所有与角色管理相关的内容都必须统一收纳到此功能点中。
   - 权限管理：二级功能，功能类别为 `页面`。用于管理系统权限、定义权限规则。
     - 原始需求中所有与权限相关的内容都必须统一收纳到此功能点中，包括但不限于：权限配置、权限验证、权限规则、权限分配等。
     - 禁止在功能模块目录中创建独立的"权限配置"、"权限验证"等重复的权限功能点。
   - 部门管理：二级功能，功能类别为 `页面`。用于管理组织部门结构、维护部门信息。
     - 原始需求中所有与部门管理相关的内容都必须统一收纳到此功能点中。

这些内置功能模块禁止被隐藏、禁止被遗漏、禁止被伪造、禁止被重复。原始需求中所有与登录、权限、用户、角色、部门、人员、组织架构相关的功能描述都必须统一收纳到对应的内置功能模块中，禁止自行发挥创建独立的功能点。 -->

<!-- 特别注意：如果出现写入生成内容失败的情况，大概率因为调用 Write 工具没有输入规范的 JSON，在此基础上进行重新生成 Write 工具的输入参数，实现写入生成内容到指定文件。或者直接调用 ``` echo ``` 命令行实现文件的写入。 -->

<!-- 示例表头：`| 一级功能 | ... | X级功能 | 功能类别 |\n`。特别注意这里的换行标识，禁止因为换行导致调用工具出现语法错误。-->
<!-- 示例表体：`| 功能模块 | ... | 详情 | 页面 |\n`。特别注意这里的换行标识，禁止因为换行导致调用工具出现语法错误。-->
<!-- 示例表格：`| 功能模块 | ... | 详情 | 页面 |\n|---------| ... |---------|---------|\n| 功能模块 | ... | 详情 | 页面 |\n`。特别注意这里的换行标识，禁止因为换行导致调用工具出现语法错误。-->

<!-- 内置功能模块示例表格：
| 一级功能 | 二级功能 | 功能类别 |
| ------- | ------- | ------- |
| 登录 | | 页面 |
| 无权限页 | | 页面 |
| 权限中心 | 用户管理 | 页面 |
| 权限中心 | 角色管理 | 页面 |
| 权限中心 | 权限管理 | 页面 |
| 权限中心 | 部门管理 | 页面 |

这个示例展示了系统必须包含的内置功能模块的标准格式。所有系统都必须包含这些内置功能模块，禁止遗漏或修改。 -->

| [功能级数] | 功能类别 |
| --------- | --------- |
| [功能名称] | [具体功能类别] |

# 隐藏内容

<!-- 功能模块目录的表格体的每一项多必须按照以下格式列出，禁止遗漏表格体的任何一项 -->

- **[多级功能合并，例如：`商品管理-***-商品列表`]**：[必须列出当前功能点隐藏的所有子功能点，禁止遗漏或虚构任何隐藏的子功能点。]
