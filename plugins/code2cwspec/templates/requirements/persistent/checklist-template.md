# [功能要点名称]

<!-- 【编辑策略】生成过程中尽量只编辑替换对应的文本段落，避免重新生成完整的文件。对于已有内容，采用精准的文本替换而非全量重写，这样可以保留已有的正确内容，提高生成效率。 -->

- **视觉需求**：[文件内部已经提供，无需进行生成。如果文件内部已经有 `视觉需求` 内容，禁止修改或删除文件内的 `视觉需求` 内容]

<!-- 阅读并充分理解 关键内容，读取当前功能要点的业务描述。 -->

[详细说明当前功能要点的业务需求]

## 验收列表

<!-- 角色权限：阅读并充分理解 术语表 的 权限角色术语表 模块，禁止伪造任何权限术语。-->

<!-- 验收类别：对子功能点的具体功能进行拆分。比如：表单校验、用户操作、数据展示、权限控制、异常处理等。一个验收功能存在多个验收类别，禁止遗漏任何可能的验收类别。 -->
<!-- 验收规则：严格按照 EARS 规则描述，详细说明当前 验收类别 下的具体功能操作，用作开发业务需求的验收标准。需要明确说明 角色权限。一个验收类别存在多个验收规则，禁止遗漏任何可能的验收规则。 -->

<!-- 验收规则-句式强制要求（优先级最高，违反即为错误）：

  【强制句式】每一条验收规则必须明确交代操作发生的页面，使用以下四种句式之一，禁止任何例外：

  句式A（操作结果留在当前页面，无跳转）：
    在**当前页面**，[描述具体操作或展示内容]。
    错误示例：「展示风险评价列表，包含字段：…」← 缺少页面主语，禁止使用
    正确示例：「在**当前页面**，展示风险评价列表，包含字段：…」

  句式B（操作触发跳转到另一个不同名称的页面）：
    在**当前页面**，点击「[按钮名]」后，**跳转到「[目标页面名]」**。
    仅当当前功能模块中确实存在该目标页面时，才允许使用“跳转到「[目标页面名]」”句式；禁止根据习惯、常识或相似功能臆造任何页面名。
    如果需求只体现查看详情、继续处理、进入下一步等动作，但当前功能模块中并不存在明确的目标页面，则禁止写跳转，必须改写为当前页面内完成、打开弹窗、打开抽屉或其他真实存在的交互方式。
    错误示例：「点击查看后跳转到详情页」← 缺少当前页面主语，禁止使用
    正确示例：「在**当前页面**，点击「查看」后，**跳转到「风险评价详情」**」

  句式C（操作触发打开弹窗）：
    在**当前页面**，点击「[按钮名]」后，**打开「[弹窗名]」弹窗**。
    正确示例：「在**当前页面**，点击「新增」后，**打开「新增风险评价」弹窗**」

  句式D（角色/权限判断）：
    当**角色「[角色名]」**在**当前页面**时，[描述展示或操作差异]。
    错误示例：「当用户是管理员时可以删除」← 角色未加粗、缺少页面，禁止使用
    正确示例：「当**角色「管理员」**在**当前页面**时，显示「删除」按钮」

  【检查方法】生成每条验收规则后，必须自查：这条规则是否包含「在**当前页面**」或明确的「**跳转到「目标页」**」？如果不包含，必须按上述句式修改后再输出。凡是写了“跳转到「目标页面名」”的规则，必须额外强制核对该目标页面是否真实存在于当前功能模块；如果无法确认真实存在或实际不存在，必须取消跳转写法并改为其他真实交互方式。
-->

<!-- 验收规则必须采用中文格式生成。 -->
<!-- 验收规则的所有内容必须是明确不含糊的，禁止出现 等等、忽略、包括但不限于 等类似含糊的字段，必须明确说明所有细节功能。比如：`人事信息包含姓名、年龄等数据` 这就是一个非常模糊的描述，必须详细说明所有细节，正确情况 `人事信息包含姓名、年龄、性别、婚姻状况、主治、紧急联系人、联系电话、生日，国籍、学籍、学历` 事无巨细说出所有实际功能细节。 -->

<!-- 验收列表的核心目标：完全替代原始需求。生成的验收列表必须包含所有原始需求中的业务细节，使得后续开发人员仅通过验收列表就能完整理解和实现功能，无需查阅原始需求文档。 -->

<!-- 【分批处理约束】为避免大模型输入超限，本验收列表仅针对当前功能要点生成。生成步骤如下：(1)阅读并充分理解当前功能要点在 [关键内容-[一级功能中文名称]] 中的业务描述；(2)根据当前功能要点的业务描述，生成完整的验收列表；(3)确保验收列表包含当前功能要点的所有业务细节、数据属性、流程步骤、权限控制、异常处理等；(4)验收列表完成后，不要继续生成其他功能要点的内容；(5)其他功能要点的验收列表将在后续请求中逐个生成。【关键约束】：(1)本次只生成当前功能要点的验收列表，禁止生成多个功能要点；(2)禁止遗漏当前功能要点的任何业务细节；(3)禁止在一次请求中尝试完成所有功能要点的验收列表。【编辑策略】生成过程中尽量只编辑替换对应的验收规则段落，避免重新生成完整的验收列表。对于已有的验收规则，采用精准的文本替换而非全量重写，这样可以保留已有的正确内容，提高生成效率。这样可以有效避免大模型输入超限，确保任务成功完成。 -->

<!-- 常见错误一：遗漏关键数据属性信息。验收规则的对关键数据属性的描述出现 `等等`、`忽略`、`包括但不限于` 类似省略的描述。错误示例：`人事信息包含姓名、年龄等数据` 这就是一个非常模糊的描述，必须详细说明所有细节。正确示例：`人事信息包含姓名、年龄、性别、婚姻状况、主治、紧急联系人、联系电话、生日，国籍、学籍、学历` 事无巨细描述出关键数据属性。 -->

<!-- 常见错误一补充：遗漏业务数据的完整清单。任何涉及数据的验收规则，必须明确说明涉及哪些业务数据及其业务含义，禁止使用"等"、"包括但不限于"等模糊表述。错误示例：`系统支持导入用户数据`。正确示例：`系统支持导入用户数据，包含用户ID、姓名、邮箱、部门、入职日期、薪资等业务信息`。 -->

<!-- 常见错误一补充2：遗漏业务模板或业务流程中的关键信息。任何涉及模板、流程、业务规则等结构化业务内容的验收规则，必须明确列出所有关键业务信息及其业务含义。错误示例：`系统支持发放信息模板`。正确示例：`系统支持发放信息模板，模板包含以下业务信息：发放标题、发放内容、发放对象（全员/部门/个人）、发放时间、优先级（高/中/低）、是否需要回执等`。 -->

<!-- 常见错误一补充3：遗漏业务系统间的数据交互内容。任何涉及系统间数据交互、业务协作的验收规则，必须明确说明交互的业务数据内容和业务流程。错误示例：`系统支持与第三方系统集成`。正确示例：`系统支持与第三方系统集成，需要交互的业务数据包括：员工基本信息、部门信息、薪资数据等，交互方式为定期同步或实时推送`。 -->

<!-- 常见错误二：遗漏功能点。验收列表中缺少原始需求中明确提到的功能点，或缺少隐含的功能点（如：数据验证、权限控制、异常处理、边界条件等），或缺少细节功能点（如：字段列表、操作步骤、交互流程等）。检查方法：逐句对比原始需求和验收列表，确保每个功能点都有对应的验收规则。 -->

<!-- 常见错误二补充：遗漏业务数据和业务流程相关的功能点。包括但不限于：(1)业务数据的完整清单（涉及哪些业务数据、数据的业务含义）；(2)业务流程中的关键步骤和决策点（流程顺序、条件分支、业务规则）；(3)业务系统间的数据交互（交互的业务数据内容、交互频率、交互方式）；(4)业务数据的有效性和一致性要求（数据验证规则、数据冲突处理、数据同步要求）；(5)业务异常和特殊情况的处理（异常流程、降级方案、人工介入点）。 -->

<!-- 常见错误三：细节被合并。多个细节被合并成一条验收规则，导致细节丢失。错误示例：`用户可以填写表单，包括姓名、年龄、性别、邮箱等信息`。正确做法：为每个字段创建单独的验收规则（如：用户可以在表单中输入姓名、用户可以在表单中输入年龄等），或创建一个"细节清单"验收规则明确列出所有字段及其属性（如：表单包含以下字段：姓名（文本，必填）、年龄（数字，必填，范围1-150）、性别（下拉菜单，必填，选项：男/女/其他）、邮箱（邮箱字段，必填，格式验证））。 -->

<!-- 常见错误四：条件分支未拆分。包含多个条件分支的验收规则未拆分。错误示例：`当用户是管理员时可以编辑和删除，当用户是普通用户时只能查看`。正确示例：为每个条件分支创建单独的验收规则（如：当用户是管理员时，可以编辑记录；当用户是管理员时，可以删除记录；当用户是普通用户时，只能查看记录）。 -->

<!-- 常见错误五：异常和边界条件遗漏。验收列表中缺少异常处理的验收规则（如：错误提示、异常流程等），或缺少边界条件的验收规则（如：最大值、最小值、空值处理等），或缺少特殊情况的验收规则（如：重复操作、并发操作、超时处理等）。检查方法：对每个功能点，思考可能的异常情况、边界条件、特殊情况，并为其创建验收规则。 -->

- **[验收功能]**：
  - **[验收类别]**：
    - [验收规则]

<!-- 示例数据，以下数据仅作示例参考对应格式，不代表真实业务场景

- **数据展示功能**：
  - **权限控制**：
    - 当**角色「普通用户」**访问时，无法访问客户分析仪表盘页面。
  - **用户操作**：
    - 点击「查看详情」按钮后，**跳转到「客户详情页」**展示完整客户信息。
    - 点击「新增」按钮后，**打开「新增客户」弹窗**，弹窗包含姓名、手机号、所属部门字段。
    - 提交成功后，**在当前页面**刷新列表并显示最新数据。
-->

<!-- **特别注意**：生成当前功能要点的验收列表以后，必须与当前功能要点的业务描述进行严格比对，必须在验收列表中完整还原该功能要点的所有业务功能。完成后，必须逐条检查所有常见错误，确保没有遗漏任何功能点或细节。检查方法：(1)逐句对比当前功能要点的业务描述和验收列表，确保每个功能点都有对应的验收规则；(2)对每条验收规则，在业务描述中找到对应的段落，确认理解无误；(3)对每条验收规则，检查是否包含模糊词汇或遗漏细节；(4)对每条验收规则，检查粒度是否合适，是否需要拆分；(5)对每条验收规则，检查是否考虑了异常和边界情况。如果任何检查项未通过，必须修改验收列表直到所有检查项都通过为止。【分批处理完成提示】：当前功能要点的验收列表生成完毕后，请停止继续生成其他功能要点的内容。如果还有其他功能要点需要生成验收列表，请在后续的新请求中逐个处理，这样可以有效避免大模型输入超限导致的任务失败。 -->

<!-- **业务数据完整性检查清单**（必须逐项检查）：验收列表必须完整包含所有业务相关的数据和流程信息，因为后续开发仅依赖验收列表，不再查阅原始需求。检查项包括：(1)业务数据清单：所有涉及数据的验收规则，必须明确列出涉及哪些业务数据及其业务含义，禁止使用"等"、"包括但不限于"等模糊表述；(2)业务流程和规则：所有涉及业务流程的验收规则，必须明确说明流程步骤、决策点、条件分支、业务规则等；(3)业务系统交互：所有涉及系统间协作的验收规则，必须明确说明交互的业务数据内容、交互频率、交互方式等；(4)业务数据有效性：所有涉及数据输入的验收规则，必须明确说明数据验证规则、数据冲突处理、数据一致性要求等；(5)业务异常处理：所有涉及异常情况的验收规则，必须明确说明异常类型、处理流程、降级方案、人工介入点等；(6)业务约束和限制：所有涉及业务约束的验收规则，必须明确说明约束条件、限制范围、特殊情况处理等。 -->
