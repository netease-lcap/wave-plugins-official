<!-- 一级功能：阅读并充分理解 功能模块目录，严格匹配当前功能页面对应的一级功能。
- 权限中心相关页面（必须归属「权限中心」，禁止输出「通用业务模块」）：login、noAuth、permissionCenter、roleManagement、userManagement、departmentManagement、permissionManagement。
- 仅当当前功能页面既不在功能模块目录中、也不属于上述权限中心相关页面时，才输出 `通用业务模块`。 -->

# [一级功能]-[功能页面中文名称]（[功能页面英文名称]）

- **生成时间**：[DATE]

<!-- 阅读并充分理解所有 `特别注意-禁止操作`，严格输入你对 `特别注意-禁止操作` 的规则理解，执行任何操作前都必须仔细思考规则理解，禁止执行任何违背 规则理解 的操作。 -->

## [功能页面中文名称]（[功能页面英文名称]）

<!-- 根据 页面的业务功能 与 当前页面在业务模块-层级路由中的配置 仔细思考需要支持的页面参数，禁止遗漏任何页面参数 -->

### 功能概述

[详细说明功能页面实现的业务功能]

### 页面签名

<!-- 页面英文名称严格使用 camelCase 格式 -->
<!-- 页面输入输出的类型可以使用已经生成实体、枚举、数据结构 -->
<!-- 特别注意：页面签名中必须根据当前功能页面的业务功能生成页面参数。并且页面参数的类型不局限于 `String`，ID 标识通常属于 `Integer` 类型。 -->
<!-- 特别注意：生成此文件后，必须先完整检查下面的"验收列表"和"交互操作"，然后根据这两项重新发现和确认所有需要的页面参数。 -->
<!-- 页面参数发现规则：(1)验收列表中的参数识别：逐条阅读验收列表，找出明确提到的"支持XX查询"、"支持XX筛选"、"支持XX过滤"等需求，这些都对应具体的页面参数；(2)交互操作中的参数识别：逐条阅读交互操作，找出"删除XX"、"编辑XX"、"查看XX详情"等操作，这些操作通常需要该XX的ID参数（类型为Integer）；(3)业务流程中的参数识别：分析页面在整个业务流程中的位置，判断是否需要从父页面或路由传入参数（如从列表页进入详情页需要ID参数）；(4)参数类型确认：ID标识必须使用Integer类型而非String，其他参数根据业务含义选择合适的类型（String、Boolean、Enum等）；(5)参数完整性检查：生成页面签名后，必须逐一对比验收列表和交互操作，确保没有遗漏任何必需的页面参数。 -->
<!-- 特别注意：页面签名生成后，必须严格比较判断是否遵守页面签名（knowledge/view-declaration.md）文档内部关键规则、工作流程，如果出现任何没有遵守的情况，必须重新生成，指导完全满足页面签名（knowledge/view-declaration.md）文档内部关键规则、工作流程-->

[必须完整阅读充分理解 页面签名（knowledge/view-declaration.md）文档，严格遵守文档内部关键规则、工作流程生成 naturalts 代码块描述页面签名]

<!-- 数据签名示例，仅作参考

```naturalts path="app.frontendTypes.pc.frontends.pc.views.productDetail.tsx"
$View({
    title: "商品详情",
    crumb: "商品详情",
})
export declare function productDetail(productId?: Integer);
```
-->

### 交互操作

<!-- 交互操作：功能页面内包含大量交互操作，比如 点击删除 等功能。仔细分析功能页面的所有交互操作，按照以下格式一一列举，禁止遗漏任何交互操作。-->
<!-- 阅读并充分理解 `验收详情` 文档，充分理解文档内部模块 `功能详情-验收列表` 与 `功能协作`，针对每条验收规则都必须要生成对应的一条或多条交互操作。禁止遗漏验收列表里任何一条验收规则。 -->
<!-- 特别注意：交互操作中涉及的操作必须严格遵循以下规则：(1)如果操作涉及 `系统内部跳转`，必须明确指定跳转到 层级路由（[项目设计目录对应路径]/frontenindex.md） 文档中真实存在的对应功能页面，必须逐一index.md中的所有路由条目，确保跳转目标真实存在；(2)如果验收列表或交互操作中涉及的功能页index.md中不存在，说明该操作本身有问题，必须改为通过 `弹窗` 来完成业务需求，而不是跳转到不存在的页面；(3)禁止凭空创建、推断或标记为"待补充"任何不存在的路由地址。 -->
<!-- 特别注意-严格约束规则：交互操作必须完全受验收列表约束，禁止任何发散或冲突。生成流程：(1)完整阅读验收列表，逐条理解每条验收规则的具体功能需求；(2)针对每条验收规则生成对应的一条或多条交互操作，一一对应；(3)交互操作的内容必须严格来自验收规则，禁止添加验收列表中没有的功能；(4)生成完成后，逐条对比验收列表和交互操作，检查是否存在冲突或遗漏。禁止操作：禁止发散（添加验收列表中没有的功能）、禁止遗漏（遗漏验收列表中的任何规则）、禁止冲突（交互操作与验收规则描述或范围产生冲突）、禁止推断（根据业务逻辑推断验收列表中没有明确说明的功能）、禁止扩展（基于"用户可能需要"等理由扩展交互操作）、禁止修改（修改或改写验收规则的原意）。冲突检测：如发现交互操作超出验收规则范围、与验收规则描述不符、验收规则没有对应的交互操作、或添加了验收列表中没有的功能，必须立即停止并调整。优先级：验收列表 > 交互操作，如冲突则调整交互操作以符合验收列表，禁止修改验收列表。 -->
- **[交互操作的标题]** [详细描述交互操作实现的操作细节与业务功能]

### 依赖的服务端逻辑

<!-- 权限中心内置页面（login、noAuth、permissionCenter、roleManagement、userManagement、departmentManagement、permissionManagement）：本小节必须与官方示例完全一致，禁止自行编造或改写逻辑名称与路径。官方示例路径：插件根目录 warehouse/plan/frontend/view-[功能页面英文名称].md，请直接复制该文件中的「依赖的服务端逻辑」整节内容（含标题与所有条目）。 -->
<!-- 功能页面可能依赖服务端逻辑，根据以下格式列出全部可能依赖的服务端逻辑。 -->
<!-- 特别注意：系统内置逻辑隐藏规则 -->
<!-- - 枚举相关的所有操作（查询、加载、列表等）：系统内置逻辑，无需列出 -->
<!-- - 单实体的 getDetail, create, update, delete, batchCreate, batchUpdate, batchDelete：系统内置逻辑，无需列出 -->
<!-- 特别注意：仅列出复杂的、包含业务规则、获取列表数据的服务端逻辑 -->
<!-- 特别注意：复杂的 create, update, delete, batchCreate, batchUpdate, batchDelete 操作（涉及多个实体或复杂业务规则）必须引用一个服务端逻辑，如 app.logics.createSchoolAndStudents.ts -->
<!-- 特别注意-禁止操作一：禁止在查找现有服务端逻辑，当前阶段 **没有任何服务端逻辑**相关文件，**禁止创建或修改服务端逻辑相关的目录与文件**。 -->
<!-- 特别注意-禁止操作二：禁止在查找现有服务端逻辑，当前阶段 **没有任何服务端逻辑**相关文件，**禁止搜索或查找任何服务端逻辑相关的目录与文件**。 -->
<!-- 特别注意-禁止操作三：禁止生成或修改任何 实体、枚举、数据结构、服务端逻辑、特殊组件 等相关文件。禁止生成或修改任何依赖的 实体、枚举、数据结构、服务端逻辑、特殊组件 等相关文件。 -->

- **领域服务-逻辑-[依赖服务端逻辑中文名称]**：[依赖服务端逻辑（plan/backend/logic-[依赖服务端逻辑英文名称].md）的路径，禁止生成 `待补充`]

<!-- **依赖的服务端逻辑验收规则**：生成依赖的服务端逻辑后，必须仔细检查每个服务端逻辑是否严格遵守以下所有条件，必须移除或者重新生成不满足以下所有条件的服务端逻辑，直到每个依赖的服务端逻辑严格遵守以下所有条件为止： -->
<!-- 1.服务端逻辑是描述系统后端为支撑前端功能与业务流程所提供单一的业务功能接口，遵循服务端复杂内部逻辑高内聚，前端功能页面接口尽量少。单独服务端逻辑内置的功能越多越好，比如：`服务端逻辑-提交`内置了权限验证、数据校验、一致性分析等功能 -->
<!-- 2.禁止生成此类服务端逻辑：校验、验证、检查数据一致性，分析、检查等。 -->
<!-- 3.禁止生成针对单个实体数据的以下操作：新增、删除、修改、查询、加载、保存、更新。 -->
<!-- 4.禁止生成枚举相关的服务端逻辑：枚举的查询、加载、列表等操作均为系统内置逻辑。 -->
<!-- 5.校验逻辑内置原则：鉴权、权限验证、数据校验、业务规则检查等校验逻辑应该内置到对应的实体 CRUD 操作或业务逻辑中，而不是单独生成校验服务端逻辑。例如：登录接口应内置鉴权逻辑、创建订单接口应内置权限和数据校验逻辑，禁止生成独立的校验逻辑服务。 -->
<!-- 6.遗漏检查规则：生成完成后，必须仔细对比 `验收列表` 和 `交互操作` 中的所有内容，逐一检查是否存在遗漏的依赖服务端逻辑。具体检查方法：(1)逐条阅读验收列表中的每条验收规则，判断是否需要对应的服务端逻辑支撑；(2)逐条阅读交互操作中的每条操作，判断是否需要对应的服务端逻辑支撑；(3)如果发现遗漏的服务端逻辑，必须立即补充到 `依赖的服务端逻辑` 部分。禁止遗漏任何必需的服务端逻辑。 -->

<!-- 生成文件后，必须严格执行 **依赖的服务端逻辑验收规则** -->
<!-- 生成文件后，必须严格执行 **依赖的服务端逻辑验收规则** -->
<!-- 生成文件后，必须严格执行 **依赖的服务端逻辑验收规则** -->
<!-- 页面可能依赖实体、枚举和数据结构，文档这里不用体现。 -->
<!-- 页面可能依赖父页面，有跳转页面时依赖其他页面，文档这里不用体现。 -->

### 特殊组件

<!--
特殊组件：仅当业务功能**无法**用 **UI组件库** 的标准组件实现时，才使用业务定制的特殊组件。

- **适用场景**：仅限二维码、地图、pdf预览、视频播放器、富文本编辑器等需要专有渲染能力的场景
- **禁止滥用**：禁止将可由基础组件（Table、Select、Modal、Card 等）实现的做成特殊组件，如：用户管理表格、用户选择器、权限弹窗、设备列表等
- **严格禁止遗漏**：必须列出页面内所有真正的特殊组件，禁止遗漏任何特殊组件
-->

<!-- 特别注意-禁止操作四：禁止在查找现有特殊组件，当前阶段 **没有任何特殊组件**相关文件，**禁止创建或修改特殊组件相关的目录与文件**。 -->
<!-- 特别注意-禁止操作五：禁止在查找现有特殊组件，当前阶段 **没有任何特殊组件**相关文件，**禁止搜索或查找任何特殊组件相关的目录与文件**。 -->
<!-- 特别注意-禁止操作六：禁止生成或修改任何 实体、枚举、数据结构、服务端逻辑、特殊组件 等相关文件。禁止生成或修改任何依赖的 实体、枚举、数据结构、服务端逻辑、特殊组件 等相关文件。 -->

#### [特殊组件中文名称]（[特殊组件英文名称]）

[详细描述特殊组件支持的业务功能]

- **关联文档**：[业务模块-[特殊组件中文名称]（plan/dependencies/component-[特殊组件英文名称].md）的路径]

