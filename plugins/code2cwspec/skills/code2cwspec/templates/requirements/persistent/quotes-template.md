# 原始引用

- **生成时间**：[DATE]

<!-- 关键规则/特别注意：严格按照以下所有步骤创建任务清单（TodoList），任务清单（TodoList）必须包含以下每个步骤对应的单独任务项，禁止合并或者压缩任何步骤的信息，然后严格遵守任务清单（TodoList）生成原引用（禁止跳过任务清单中任何任务项）： -->
<!-- 步骤一：充分理解并严格遵守 `关联段落匹配规则一`，通过关键字匹配到 `关联主题` 对应的所有文本。 -->
<!-- 步骤二：充分理解并严格遵守 `关联段落匹配规则二`，通过原始需求的文本内容匹配到 `关联主题` 对应的所有段落。 -->
<!-- 步骤三：暂时忽略 `关联段落匹配规则三`、`关联段落合并规则`，生成 `原始引用` 到目标文件。 -->
<!-- 步骤四：充分理解并严格遵守 `关联段落匹配规则三`，剔除前面步骤匹配的 `关联段落` 中的开发技术相关内容。 -->
<!-- 步骤五：充分理解并严格遵守所有 `关联段落合并规则`，严格执行每条关联段落合并规则，仅对符合规则条件的 `关联段落` 进行合并。 -->
<!-- 步骤六：充分理解并严格检查所有 `常见错误`，必须针对每条常见错误对已生成的 `关联段落` 严格进行逐一验证，如果出现任何常见错误，必须重新生成原始引用，直到完全规避所有常见错误为止。 -->
<!-- 步骤七：必须逐个严格检查 项目背景、`权限角色`、`整体业务` 的关联段落，必须移除内部开发技术相关的内容。 -->

<!-- 关联段落：指 原始需求 中跟 关联标题 相关的文本段落，一般会有一处或者多处，禁止遗漏任何 关联段落。 -->
<!-- 关联段落格式：**[L[关联段落在原始需求中起始行号],[关联段落在原始需求中结束行号]]([原始需求（spec.md 或 spec.txt）的路径]#L[关联段落在原始需求中起始行号],[关联段落在原始需求中结束行号])**：[段落内容简短描述，15字左右] -->

<!-- 关联段落匹配规则一：基于 关联标题 名称衍生多个关键字，并逐个对所有衍生关键字进行搜索匹配，命中的一行或者多行就属于 关联标题 的 关联段落。 -->
<!-- 关联段落匹配规则二：原始需求中文本描述进行匹配查找符合 关联标题 的段落，可能存在一处或者多处，禁止遗漏任何 关联段落。 -->
<!-- 关联段落匹配规则三：禁止匹配任何开发技术相关的关联段落，开发技术对于我们来说只是干扰。比如：数据架构、技术架构等。 -->
<!-- 关联段落匹配规则四：已经匹配到的关联段落可以拓展关联主题的业务范围，并在此基础上通过原始需求中文本描述，匹配更多的 关联段落。 -->

<!-- 关联段落合并规则一：如果两个或者多个 关联段落 的行号是前后连续的，这样的多个 关联段落 可以合并成一个 关联段落，比如：`[L199,200]` 与 `[L201,300]` 就可以合并成 `[L199,300]` 作为 关联段落 。 -->
<!-- 关联段落合并规则二：如果两个或者多个 关联段落 的行号存在包含情况，这样的多个 关联段落 可以合并成一个 关联段落，比如：`[L250,251]` 与 `[L240,261]` 就可以合并成 `[L240,261]` 作为 关联段落 。 -->

<!-- 常见错误一：关联段落严重遗漏关键字在原始需求中匹配到的文本段落。 -->
<!-- 常见错误二：因为内容重复擅自忽略匹配到的关联段落。比如：关联段落一 是 关联标题 对简单描述，关联段落二 是 关联标题 对详细描述。必须同时保留两个关联段落，禁止因为 关联段落二 的内容包含 关联段落一，就把 关联段落一 忽略。 -->
<!-- 常见错误三：功能模块目录的功能点下只有关键字匹配到的关联段落，没有通过 关联段落匹配规则二 匹配到复合 关联主题 的原始文本段落。 -->
<!-- 常见错误四：忽略 关联段落匹配规则四，没有再已有关联段落的基础上，引申匹配更多的关联段落。 -->

<!-- 以下的 `项目背景`、`权限角色`、`整体业务` 都属于 关联段落 对应的 关联标题。 -->

- **项目背景**：
  - [严格遵守 关联段落匹配规则 与 关联段落格式 生成 关联段落]
- **权限角色**：
  - [严格遵守 关联段落匹配规则 与 关联段落格式 生成 关联段落]
- **整体业务**：
  - [严格遵守 关联段落匹配规则 与 关联段落格式 生成 关联段落。整体业务聚焦与项目的总体规划，不涉及过于细节的功能描述。禁止通过关键字进行匹配。禁止整体业务局限与某个子系统，导致忽略原始需求中对系统其他模块架构的描述]

<!-- 将功能模块目录中的表格每一行表格项作为系统功能点，按照以下格式生成关联段落，禁止遗漏或伪造任何功能模块目录的表格项。由于功能模块目录数据可能过多，必须逐个生成所有表格项的关联段落，你有充足的时间处理，禁止压缩任何内容，严格执行所有关联段落匹配规则。 -->

-**[多级功能合并，例如：`商品管理-***-商品列表`]**：
  - [严格遵守 关联段落匹配规则 与 关联段落格式 生成 关联段落]