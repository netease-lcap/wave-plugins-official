# 关键业务流程

- **生成时间**：[DATE]

<!-- 关键业务流程：必须先充分理解 `关键内容` 与 `功能模块目录`，只允许基于这两个输入提取整个需求中最核心、最关键、最不能失败的 P0 级业务链路。 -->
<!-- 关键业务流程不是功能点清单，也不是页面清单，而是“从业务触发到业务结果达成”的端到端核心链路。 -->
<!-- 特别注意：当前阶段很多标准文档还没有生成，禁止依赖 功能协作、功能要点、页面设计、技术设计 等尚未产出的文档内容做反向补充。 -->
<!-- 关键业务流程必须满足以下标准： -->
<!-- 1. 该流程一旦失败，会直接导致当前需求的核心业务目标无法成立。 -->
<!-- 2. 该流程必须体现明确的起点、关键处理过程、终点结果，禁止只写零散功能动作。 -->
<!-- 3. 该流程必须覆盖跨角色、跨页面、跨服务端逻辑、跨数据对象之间的核心传递关系，禁止只写局部操作。 -->
<!-- 4. 只提取最核心的 P0 级业务链路，禁止把低优先级、边角型、补充型、管理型、配置型、统计型流程混入关键业务流程。 -->
<!-- 5. 如果多个功能点本质上属于同一条业务主链路，必须合并为一条完整流程，禁止机械拆碎。 -->
<!-- 6. 每条关键业务流程都必须能够回答：谁，在什么触发条件下，完成了什么关键业务动作，最终达成什么核心业务结果。 -->
<!-- 7. 关键业务流程通常不止一条；只要需求中存在多条不同的 P0 级主业务链路，就必须全部提取出来，禁止只生成一条流程就结束。 -->

<!-- 功能来源：关键业务流程中出现的功能模块、角色、页面、单据、状态、业务对象都必须来自 `关键内容` 与 `功能模块目录`，禁止伪造。 -->
<!-- 提取方式：必须先基于 `功能模块目录` 明确业务范围，再回到 `关键内容` 提取每个业务范围中的主业务闭环，禁止脱离功能模块目录直接散乱归纳。 -->
<!-- 读取顺序：禁止试图一次性读取并归纳所有功能点；必须按功能点一条一条读取、理解、判断，再逐条补充关键业务流程。 -->
<!-- 完整性要求：必须逐个功能模块、逐段关键内容检查是否还存在其他独立成立的 P0 级主业务链路；只要还有一条关键业务流程未提取，就不得结束。 -->
<!-- 术语约束：必须充分理解 `术语表`，关键业务流程中出现的角色名称、业务对象名称、单据名称、状态名称都必须与术语表严格对齐，禁止擅自改写、缩写、替换或伪造术语。 -->
<!-- 页面约束：关键业务流程中出现的页面、弹窗、抽屉、页签、功能模块，必须与 `功能模块目录` 一一对齐；禁止生成 `功能模块目录` 中不存在的页面或功能模块名称。 -->
<!-- 角色约束：关键业务流程中出现的角色，必须与 `术语表` 中的权限角色术语表、权限数据表严格一一对齐；禁止出现未定义的权限角色，禁止臆造任何角色身份。 -->
<!-- 流程粒度：关键业务流程必须是“主业务链路”粒度，至少包含 5 个以上连续步骤，禁止退化成一句话概述。 -->
<!-- 流程步骤：每一步必须写清楚执行角色、关键动作、操作对象/业务对象、产生结果，禁止出现“进行处理”“完成操作”等含糊表达。 -->
<!-- 加重显示规则：每一步中涉及的关键角色、触发动作、页面/弹窗/抽屉/功能模块名称、关键业务对象、目标结果，都必须使用 Markdown 加粗格式 `**` 突出显示。 -->
<!-- 示例格式：点击「新增」按钮后，**打开「新增客户」弹窗**，弹窗包含姓名、手机号、所属部门字段。 -->
<!-- 终局结果：每条关键业务流程都必须写清楚流程完成后的核心业务结果与业务价值。 -->
<!-- 异常约束：必须补充流程中最关键的失败阻断点，说明哪些条件不满足时流程不能继续推进。 -->
<!-- 生成方式：禁止尝试一次性生成全部关键业务流程文件内容；因为这样可能导致上下文超限、生成失败或速度过慢。必须采用增量生成方式，每次只处理当前正在读取的功能点对应的关键业务流程。 -->
<!-- 写入方式：每次生成新的关键业务流程时，必须从目标文档尾部直接追加补充，禁止整篇重写、禁止一次性覆盖生成所有流程。 -->

## [关键业务流程名称]

- **流程目标**：[用一句话说明这条流程要达成的核心业务目标，必须是整个需求中的 P0 级目标]
- **触发角色**：[发起该流程的角色名称]
- **协作角色**：[参与该流程的其他角色名称；如果没有则写“无”]
- **触发条件**：[什么业务前提下会启动这条流程]
- **起点**：[流程从哪个业务动作或业务状态开始]
- **终点**：[流程最终达成什么业务结果或落到什么业务状态]

### 核心业务链路

1. [步骤一：必须对 **触发角色**、**触发动作**、**页面/弹窗/功能模块**、**关键业务对象**、**处理结果** 做加粗显示]
2. [步骤二：必须对 **触发角色**、**触发动作**、**页面/弹窗/功能模块**、**关键业务对象**、**处理结果** 做加粗显示]
3. [步骤三：必须对 **触发角色**、**触发动作**、**页面/弹窗/功能模块**、**关键业务对象**、**处理结果** 做加粗显示]
4. [步骤四：必须对 **触发角色**、**触发动作**、**页面/弹窗/功能模块**、**关键业务对象**、**处理结果** 做加粗显示]
5. [步骤五：必须对 **触发角色**、**触发动作**、**页面/弹窗/功能模块**、**关键业务对象**、**处理结果** 做加粗显示]

### 关键结果

- [说明流程完成后直接产出的核心业务结果]
- [说明该结果对整体需求目标的关键价值]

### 关键阻断点

- [阻断点一：如果什么条件不满足，流程必须中断，不能继续]
- [阻断点二：如果什么校验失败，流程必须中断，不能继续]

<!-- 特别注意：关键业务流程可能会有多条，绝不是默认只有一条；必须尽量完整提取当前需求中的所有关键业务流程，禁止只生成一条流程就结束。 -->
<!-- 特别注意：如果需求中存在多条关键业务流程，必须按上述格式逐条展开，禁止遗漏。 -->
<!-- 特别注意：关键业务流程必须完整覆盖“主链路”，禁止只写某个页面操作片段，也禁止把多个无关链路硬拼成一条流程。 -->
<!-- 特别注意：禁止输出“待确认”“等等”“相关信息”“后续处理”等模糊描述，必须把关键动作、关键对象、关键结果说清楚。 -->
<!-- 特别注意：如果 `关键内容` 与 `功能模块目录` 无法支撑某条流程成立，就不要臆造流程，必须严格以这两个输入为准。 -->
<!-- 特别注意：凡是角色名称、页面名称、功能模块名称、弹窗名称、关键动作、目标结果，只要属于当前步骤的关键信息，就必须加粗显示，禁止普通文本平铺。 -->
<!-- 特别注意：页面必须与 `功能模块目录` 一一对齐；角色必须与 `术语表` 中的权限角色术语表、权限数据表一一对齐；术语名称必须严格遵守 `术语表`，禁止自造页面名、自造角色名、自造术语名。 -->
<!-- 特别注意：不要一次写出所有功能点，必须按功能点逐条读取、逐条判断、逐条补充。 -->
<!-- 特别注意：不要尝试一次生成所有关键业务流程内容；必须每次从文档尾部直接补上当前新增的关键业务流程，避免上下文超限和生成过慢。 -->
