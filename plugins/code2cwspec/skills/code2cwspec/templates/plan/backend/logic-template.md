<!-- 归属子域：阅读并充分理解 应用架构-核心领域划分，通过文档匹配当前服务端逻辑归属的核心子域，如果没有当前服务端逻辑的归属的核心子域，直接输出 `通用领域服务` -->

# [归属子域]-[服务端逻辑中文名称]（[服务端逻辑英文名称]）

- **生成时间**：[DATE]

<!-- 服务端逻辑：描述系统后端为支撑前端功能与业务流程所提供单一的业务功能接口，比如：提交数据等。 -->

## [服务端逻辑中文名称]（[服务端逻辑英文名称]）

### 功能概述

[详细说明服务端逻辑包含的业务功能]

### 功能要点

<!-- 功能要点：当前业务逻辑需要实现的业务功能点。 -->

- **[功能要点标题]**：[功能要点详细描述，至少 100 字]

### 逻辑签名

<!-- 逻辑英文名称严格使用 camelCase 格式 -->
<!-- 逻辑输入输出的类型可以使用已经生成实体、枚举 -->

<!-- 特别注意：逻辑签名生成后，必须严格比较判断是否遵守逻辑签名（knowledge/logic-declaration.md）文档内部关键规则、工作流程，如果出现任何没有遵守的情况，必须重新生成，指导完全满足逻辑签名（knowledge/logic-declaration.md）文档内部关键规则、工作流程 -->

<!-- 特别注意：逻辑签名生成后，必须严格比较严格根据常见错误对逻辑签名生成代码进行严格检查，禁止出现与见错误类似的问题。如果出现类似错误，必须改正逻辑签名，直到完全规避所有常见错误为止。 -->

<!-- 常见错误：一些输入参数（类似：状态、条件、分类等类似的参数名称，这些在 **数据建模-枚举** 里面都有专门对应的枚举定义）的类型定义根据 业务说明/技术设计 应该定义为 **枚举类型**，但是错误判断并定义为 `String` 或者其他简单类型，导致业务流程出现严重错误。 -->

[必须完整阅读充分理解 逻辑签名（knowledge/logic-declaration.md）文档，严格遵守文档内部关键规则、工作流程生成 naturalts 代码块描述逻辑签名]

<!-- 逻辑签名示例数据，仅供参考

```naturalts path="app.logics.loadProductList.ts"
/* 从数据库中获取商品列表 */
export declare function loadProductList(page: Integer, size: Integer, sort: String, order: String, filter: app.dataSources.defaultDS.entities.Product): { list: List<{ customer: app.dataSources.defaultDS.entities.Product, user: app.dataSources.defaultDS.entities.LcapUser }>, total: Integer };
```

| 输入参数 | 标题 | 数据类型 | 描述 |
| --- | --- | --- | --- |
| page | 页码 | Integer | 当前请求的页码，从1开始计数 |
| size | 每页条数 | Integer | 每页返回的记录数量，支持10/20/50三种选项 |
| sort | 排序字段 | String | 用于排序的字段名称，如customerName、creationDate等 |
| order | 排序顺序 | String | 排序方向，asc表示升序，desc表示降序 |
| filter | 过滤条件 | app.dataSources.defaultDS.entities.Product | 包含商品来源、商品状态、跟进状态、商品优先级等筛选条件的商品实体 |

| 返回值字段 | 标题 | 数据类型 | 描述 |
| --- | --- | --- | --- |
| list | 商品列表 | List<{ customer: app.dataSources.defaultDS.entities.Product, user: app.dataSources.defaultDS.entities.LcapUser }> | 商品列表，每个列表项包含商品实体和对应的用户实体 |
| total | 总条数 | Integer | 符合筛选条件的商品总数量，用于分页计算 |

-->

### 被前端调用

<!-- 所有功能页面必须要再业务模块文档中有对应模块，禁止伪造任何功能页面数据 -->
<!-- 参考以下格式，列出业务模块中所有会用到服务端逻辑的功能页面，禁止遗漏 -->
- **[功能页面中文名称]（[功能页面英文名称]）**：[描述该功能页面如果使用当前服务端逻辑，以及使用当前服务端逻辑实现的业务功能。]

### 依赖的枚举、实体

<!-- 服务端逻辑可能依赖枚举、实体，根据以下格式列出全部依赖的枚举、实体 -->
<!-- 特别注意：枚举、实体已经确定，禁止伪造任何不存在的枚举、实体；实体英文名称严格使用 PascalCase 格式 -->
<!-- 【关键工作流程】必须完整阅读所有依赖实体的完整属性定义，逐一检查属性是否能完整支撑当前服务端逻辑的所有业务功能；对照"功能要点"检测现有实体属性是否满足每一个功能要点；如果现有实体属性无法满足某个功能要点，必须在下方列出所需的新的依赖实体；禁止生成任何数据结构相关的文件 -->
<!-- 强制要求：所有枚举都统一维护在 plan/data-model/enums.md 这一个文件内；这里的依赖路径也必须始终指向该文件，禁止生成、引用或依赖任何单独的枚举详情文件，例如 enum-*.md、enum-[枚举英文名称].md、enum-[枚举中文名称].md 等路径都一律不允许出现 -->

- **数据建模-枚举-[依赖枚举中文名称]**：[数据建模-枚举（plan/data-model/enums.md）的路径]
- **数据建模-实体-[依赖实体中文名称]**：[数据建模-实体-[依赖实体中文名称]（plan/data-model/entity-[依赖实体英文名称].md）的路径]

### 依赖的外部能力

[文件内部已经提供，无需进行生成。如果文件内部已经有 `依赖的外部能力` 内容，禁止修改或删除文件内的 `依赖的外部能力` 内容]
