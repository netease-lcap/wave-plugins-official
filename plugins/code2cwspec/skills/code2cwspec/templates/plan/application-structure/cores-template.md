# 应用架构-核心领域划分

- **生成时间**：[DATE]

<!-- 核心领域划分：是对规范需求进行分析理解，对规范需求描述的系统业务能力按高内聚、低耦合原则，拆分为若干**界限上下文**（Bounded Context），每个上下文聚焦一个独立业务目标，拥有专属数据模型与业务规则，并通过明确接口与其他上下文交互。

【核心原则】
1. 实体独立性：每个核心实体必须拥有独立的生命周期管理权、唯一标识符、业务规则和存储边界。跨子域的同名或相似实体必须明确其独立职责，禁止出现功能重叠或包含关系。
2. 属性隔离：任何仅作为他者特征、描述性数据、枚举值、关联标识的概念（如价格、类型、日期、状态、数量等）严禁列为核心实体。
3. 映射关系处理：多对多关系的映射表仅在确实需要额外业务逻辑时才作为实体，否则应作为关联关系处理。
4. 权限模块强制性：系统中所有与用户、权限、角色、组织架构、部门相关的功能必须统一交由内置的权限中心（PermissionCenter）实现，禁止在其他子域中重复实现或变相实现这些功能。
5. 核心领域独立性：禁止自行生成与权限中心（PermissionCenter）功能重叠的核心领域，禁止创建任何变相的权限管理、用户管理、角色管理、部门管理子域。
6. 人员信息统一管理：与当前组织架构相关的所有人员相关信息（包括人员实体、人员领域划分、人员管理功能等）属于权限中心内置模块，禁止在业务子域中重复定义或管理。
-->

[结合需求文档，详细说明核心领域划分面对实际业务系统的划分思路，并说明多个业务子域之间的关联关系。]

## [子域中文名称]（[子域英文名称]）

<!-- 严格区分实体与属性：核心实体须有独立生命周期和唯一标识；仅描述他者特征、作为枚举/字典/关联项的概念（如类型、价格、时间、日期、类别等）均为属性，严禁列为实体。自检：若对象脱离主对象无法独立管理或仅为特征，则归为属性。仅输出真正的核心实体。

【实体与属性的判别标准】
- ✅ 核心实体：拥有独立的生命周期管理、唯一标识符、业务规则、存储边界
- ❌ 属性/关联：仅为描述性数据、枚举值、计算值、关联标识、状态标记
- ❌ 禁止项：单价、价格、数量、类型、状态、日期、时间、名称、描述、标签等描述性概念
- ❌ 禁止项：任何以"属性"、"值"、"标记"、"标签"、"配置"命名的概念（除非有独立业务规则）
-->

- **职责**：[详细说明该子域的核心业务目标。]
- **核心实体**：[列出该子域管理的关键数据实体（禁止出现类似实体属性、枚举的情况），实体名称格式采用：`中文名称（英文名称）`。⚠️ 严格禁止：禁止创建任何人员相关的实体（如员工、职员、人员、人员信息等），禁止创建任何人员角色实体（如网格员、经理、主管、负责人等"XXX员"、"XXX人"的实体），禁止创建任何与组织架构相关的实体（如部门、组、团队、部门分组、组织单元等组织结构相关的实体），禁止创建任何与组织架构相关的人员实体，所有人员和组织架构相关功能必须使用内置的权限中心（PermissionCenter）提供的 LcapUser 和 LcapDepartment 实体。]
- **实体独立性说明**：[若该子域的实体与其他子域存在同名或功能相似的实体，必须明确说明各自的独立职责、管理边界、生命周期差异，禁止出现功能重叠或包含关系。]

【严重问题-属性混淆】
❌ 禁止：将实体的属性、特征、计算值当作核心实体列出
❌ 禁止：商品单价、订单金额、用户名称、商品分类、订单状态、支付方式等
❌ 禁止：任何以"属性"、"值"、"配置"、"参数"、"标记"、"标签"结尾的概念
✅ 正确：仅列出拥有独立生命周期、唯一标识、业务规则的核心实体

【严重问题-实体重复】
❌ 禁止：多个子域出现功能重叠、包含关系的同名或相似实体
❌ 禁止：如"商品"在商品子域和库存子域中重复出现，且职责不清
❌ 禁止：一个实体的属性被另一个子域当作核心实体管理
✅ 正确：若多个子域涉及同一概念，必须明确各自的独立职责、管理边界、生命周期
✅ 正确：通过明确的接口和事件进行跨域协作，而非重复管理

【严重问题-权限相关功能重复】
❌ 禁止：在任何子域中实现用户管理、权限管理、角色管理、部门管理、账号管理、人员管理功能
❌ 禁止：创建"用户子域"、"权限子域"、"角色子域"、"组织架构子域"、"账号子域"、"人员子域"等变相权限管理领域
❌ 禁止：自行定义用户、权限、角色、部门、账号、人员等权限相关实体
❌ 禁止：创建任何人员角色实体（如网格员、经理、主管、负责人等"XXX员"、"XXX人"的实体）
❌ 禁止：创建任何与组织架构相关的实体（如部门、组、团队、部门分组、组织单元等组织结构相关的实体）
❌ 禁止：实现登录、认证、鉴权等权限中心已有的功能
❌ 禁止：在业务子域中重复定义或管理与当前组织架构相关的人员信息
✅ 正确：所有权限、用户、角色、部门、账号、人员、组织架构相关功能必须统一使用权限中心（PermissionCenter）
✅ 正确：如需表示人员的特定角色或职责，通过权限中心的角色机制或直接关联 LcapUser
✅ 正确：其他子域仅可通过权限中心提供的接口进行权限查询和验证

【实体独立性检查清单】
1. 该实体是否拥有独立的生命周期管理权？（创建、更新、删除）
2. 该实体是否拥有唯一的业务标识符？
3. 该实体是否拥有独立的业务规则和验证逻辑？
4. 该实体是否拥有独立的存储边界？
5. 若与其他子域的实体同名，是否明确了各自的职责差异？
6. 该实体是否可以脱离其他实体独立存在和管理？
7. 该实体是否与权限中心（PermissionCenter）的功能重叠？（如果是，则禁止定义）
若任何一项答案为"否"，则该概念应作为属性或关联处理，而非核心实体。

<!-- 权限中心（PermissionCenter）：属于内置的通用权限功能模块，禁止修改。 -->
<!-- 【强制性要求】系统中的所有用户、权限、角色、组织架构、部门、账号、人员及与当前组织架构相关的人员信息相关功能必须完全交由权限中心（PermissionCenter）实现。禁止在其他子域中重复实现、变相实现或部分实现这些功能。禁止自行生成与权限中心功能重叠的核心领域。 -->

## 权限中心（PermissionCenter）

- **职责**：实现需求涉及 登录、认证、鉴权、权限管理、用户管理、角色管理、部门管理、组织架构管理 等所有权限相关功能。
- **核心实体**：用户（LcapUser）、角色（LcapRole）、权限（LcapPermission）、资源（LcapResource）、部门（LcapDepartment）、角色与权限映射（LcapRolePerMapping）、用户与角色映射（LcapUserRoleMapping）、权限与资源映射（LcapPerResMapping）、用户与部门映射（LcapUserDeptMapping）
