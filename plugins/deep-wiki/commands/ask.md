---
description: 使用 Wiki 上下文和源文件引用询问有关仓库的问题
---

# Deep Wiki: 仓库问答

回答有关此仓库的问题，并以实际源代码为依据。

## 源码仓库解析（必须首先执行）

在回答之前，解析源码仓库上下文：

1. **检查 git remote**：运行 `git remote get-url origin`
2. **询问用户**：_"这是一个仅限本地的仓库，还是你有源码仓库 URL？"_
   - 远程 URL → 存储为 `REPO_URL`，使用链接引用：`[文件:行号](REPO_URL/blob/BRANCH/文件#L行号)`
   - 本地 → 使用 `(文件路径:行号)`
3. **确定默认分支**：运行 `git rev-parse --abbrev-ref HEAD`
4. **在解析之前，不要继续**

## 问题

$ARGUMENTS

## 流程

1. **检测问题语言**并以**相同语言**回答
2. **搜索**代码库中与问题相关的文件
3. **阅读**这些文件以收集证据
4. **综合**一个完全建立在实际代码之上的答案 — 绝不虚构或猜测

## 响应格式

```markdown
## [简明答案标题]

[1-2 段直接回答]

### 工作原理
[详细解释，包含行内代码引用，且当答案涉及架构、流程或关系时，至少包含 1 个 Mermaid 图表]

### 关键文件
| 文件 | 用途 | 源码 |
|------|---------|--------|
| `src/path/file.ts` | [在系统中的角色] | [链接引用] |

### 代码示例
<!-- Source: 文件路径:行号 -->
[来自实际源码的相关片段，如果有帮助]

### 相关
- [相关的概念或要探索的文件]
```

## 规则

- 仅使用此仓库中实际源文件的信息
- 绝不虚构、猜测或使用外部知识
- 始终使用解析后的引用格式行内引用源文件：
  - **远程**：`[src/path/file.ts:42](REPO_URL/blob/BRANCH/src/path/file.ts#L42)`
  - **本地**：`(src/path/file.ts:42)`
- 逐步思考复杂问题
- 如果信息不足，请明确说明并建议检查哪些文件
